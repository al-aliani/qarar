# دليل الإطلاق التشغيلي — منصة «قرار»

> الحالة: كل الإصلاحات الكودية على فرع `fix/launch-readiness-2026-07-19` (3 commits، 1652/1652 اختبار أخضر، غير مدموج).
> هذا الدليل يغطي **ما لا يُنفَّذ من الكود** — يُضبط على Supabase / GitHub / لوحات المزودين. نفّذ الأقسام بالترتيب.

آخر تحديث: 19 يوليو 2026.

---

## 0) دمج الفرع
```bash
git diff main..fix/launch-readiness-2026-07-19   # راجع
git checkout main && git merge fix/launch-readiness-2026-07-19
```
> ملاحظة: توجد تعديلات جلسة سابقة غير ملتزمة (ProjectManager/DataService/… ميزة «نسخ الفرع») خارج هذا الفرع — احسمها منفصلةً.

---

## 1) الترحيلات (Migrations) — الترتيب حرِج
1. **اضبط أسرار CI الثلاثة في GitHub** (Settings → Secrets)، وإلا يتخطّى سير النشر الترحيلات **بصمت** (تحذير بلا فشل):
   - `SUPABASE_ACCESS_TOKEN`
   - `SUPABASE_PROJECT_REF`
   - `SUPABASE_DB_PASSWORD`
2. **طبّق الترحيلات الجديدة**، وحرِجٌ أن يُطبَّق `20260719000000_stripe_payment_intent_for_refunds.sql` **قبل** نشر `webhook-stripe` المحدّث (يكتب عمود `provider_payment_intent`؛ غيابه يرفض تحديث الدفع كاملاً → 500).
   - `20260719000000_stripe_payment_intent_for_refunds.sql` (عمود ربط استرداد Stripe)
   - `20260719010000_events_props_size_limit.sql` (حدّ حجم props)
   - `20260719020000_protect_profile_privileged_columns.sql` (قفل ترقية `subscription_tier`)

---

## 2) إثبات عزل البيانات (RLS) على قاعدة الإنتاج
شغّل في SQL Editor على قاعدة **الإنتاج** نفسها:
```sql
-- كلها يجب أن تكون true
select relname, relrowsecurity from pg_class
where relname in ('studies','profiles','orders','wallet_transactions',
                  'phone_otp_challenges','events','coupons');

-- تأكيد تريغر قفل الأعمدة المميّزة
select tgname from pg_trigger where tgrelid = 'public.profiles'::regclass;
-- يجب أن يظهر: protect_profile_privileged_columns
```
> مفتاح `anon` مكشوف علناً بحكم التصميم؛ إن كانت RLS منطفئة فأي زائر يقرأ/يعدّل بيانات كل العملاء بطلب واحد. لا تُطلق قبل إثبات `relrowsecurity = true` لكل الجداول أعلاه.

---

## 3) أسرار Edge Functions (بمفاتيح **live**)
اضبطها في Supabase → Edge Functions → Secrets:

| السرّ | تحتاجه الدوال | ملاحظة |
|---|---|---|
| `MOYASAR_SECRET_KEY` / `MOYASAR_WEBHOOK_SECRET` | create-checkout, webhook-moyasar | مدى/Apple Pay/STC |
| `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` | create-checkout, webhook-stripe | البطاقات الدولية |
| `WHATSAPP_ACCESS_TOKEN` / `WHATSAPP_PHONE_NUMBER_ID` / `WHATSAPP_TEMPLATE_NAME` / `WHATSAPP_TEMPLATE_LANG` | send-sms-hook, whatsapp-otp-* | Meta Cloud API |
| `OTP_HASH_SECRET` / `SEND_SMS_HOOK_SECRET` | دوال OTP | HMAC للرموز |
| `APP_ORIGIN` | create-checkout | روابط العودة بعد الدفع |
| `GOOGLE_PLACES_API_KEY` | places-nearby | حصص/تخزين مؤقت مستحسن |
| `SUPABASE_URL` / `SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` | عام | لا تُسرّب service_role للواجهة أبداً |
| `TAMARA_API_TOKEN` / `TAMARA_NOTIFICATION_TOKEN` | create-checkout, webhook-tamara | **فقط عند تفعيل تمارا** (+ نشر webhook-tamara) |

> ⚠️ **`ALLOW_LOG_OTP` يجب ألا يساوي `'true'` في الإنتاج** — لو بقي من الاختبار، يُسجَّل رمز الدخول في سجلّ الدالة، فمن يقرأ السجلات يدخل بأي حساب. اتركه غير مضبوط.

---

## 4) تسجيل الدخول (OTP واتساب) — باب الدخول الحي الوحيد
1. فعّل **Phone provider** في Supabase Auth.
2. فعّل **Send SMS Hook** موجّهاً إلى دالة `send-sms-hook`.
3. اضبط اعتمادات Meta WhatsApp (Access Token, Phone Number ID, قالب رسالة **معتمَد**).
4. تأكّد `ALLOW_LOG_OTP` غير مضبوط.
5. **اختبر:** تسجيل دخول بجوال حقيقي → وصول الرمز عبر واتساب → دخول ناجح.
> بريد/كلمة المرور مبنيّ لكنه غير موصول؛ إن أردته حياً فهو قرار منفصل (وصل `AuthModalStub`).

---

## 5) المدفوعات — روابط Webhook + اختبار Sandbox إلزامي
سجّل عناوين webhook لدى كل مزود (استبدل `<ref>` بمرجع مشروعك):
- Moyasar → `https://<ref>.supabase.co/functions/v1/webhook-moyasar`
- Stripe → `https://<ref>.supabase.co/functions/v1/webhook-stripe` (وانسخ `STRIPE_WEBHOOK_SECRET` من Stripe)
- Tamara (لاحقاً) → `https://<ref>.supabase.co/functions/v1/webhook-tamara`

**اختبار sandbox لكل مزود قبل مفاتيح live (يشمل الاسترداد — لم يُختبَر محلياً):**
- [ ] أنشئ دراسة، سمِّها، ادفع → الطلب `paid`، التصدير مفتوح، المبلغ = السعر المعلن بالضبط (شامل الضريبة: 299/1999/4999).
- [ ] أصدر استرداداً من لوحة المزوّد → الطلب يصبح `refunded` **والوصول ينسحب** (`hasActivePayment` = false).
- [ ] أعد إرسال حدث الدفع يدوياً (Stripe) → لا 500، لا ازدواج (idempotency).
- بدّل لمفاتيح **live** فقط بعد نجاح كل ما سبق.

---

## 6) قرارات المالك قبل الإطلاق
- [ ] **منصة نشر واحدة:** احذف `vercel.json` أو `netlify.toml` (كلاهما موجود؛ نظافة نشر).
- [ ] **491MB دراسات** خارج حزمة النشر → تخزين كائني (Supabase Storage)، وعدّل روابط `web/public/data/ready-studies.json` و`database-files.json`.
- [ ] **هوية التاجر + فاتورة ZATCA:** أضف السجل التجاري + الرقم الضريبي + العنوان (تُعرض في الفوتر/about/contact)، وفعّل فاتورة ضريبية مبسّطة بعد الدفع.
- [ ] **بريد رسمي** على النطاق بدل `bin.sahib.est@gmail.com`.
- [ ] **ربط تمارا:** انشر `webhook-tamara` + اضبط سرّيها (وإلا أخفِ زرّها).
- [ ] **Sentry** (اختياري): احزمه عبر npm (لا CDN المحجوب) + أضف مضيف ingest إلى `connect-src` في CSP.
- [ ] **2FA في دخول الجوال** (كود متبقٍ): نفّذ تحدّي AAL في `PhoneAuthModal` قبل `onSuccess` (نمط `AuthModalStub.js:208-247`) — **مع اختباره بمستخدم مفعِّل 2FA**؛ يُفضَّل تنفيذه مع إعداد OTP لاختبارهما معاً.
- [ ] **paywall الباقة الذاتية** خادمي (اختياري): توليد التقارير المدفوعة يجري في المتصفح فيمكن تجاوزه تقنياً؛ المنع الكامل يتطلب Edge Function للتوليد. أو اقبل «نظام الشرف» للإطلاق.

---

## 7) تحقّق نهائي قبل أول عميل
- [ ] RLS مُثبت (قسم 2).
- [ ] كل أسرار الإنتاج مضبوطة بمفاتيح live، و`ALLOW_LOG_OTP` غير مضبوط.
- [ ] OTP واتساب يعمل end-to-end (جوال حقيقي).
- [ ] دفع + استرداد sandbox ناجحان لكل مزود مُفعَّل.
- [ ] الترحيلات الثلاثة مُطبَّقة (وترتيب Stripe صحيح).
- [ ] منصة نشر واحدة، والصفحات القانونية + هوية التاجر مكتملة.
- [ ] الأسعار المعروضة = المحصّلة (299/1999/4999 شاملة الضريبة).
