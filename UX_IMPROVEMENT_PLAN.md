# خطة تحسين تجربة المستخدم (UX Improvement Plan)

بناءً على ملاحظات بأن النظام يبدو "صعباً" ومعقداً، تم تحليل رحلة المستخدم الحالية ووضع خطة شاملة لتبسيط التجربة وجعلها أكثر بديهية، مستلهمة من أفضل منصات الـ SaaS العالمية (مثل TurboTax و Canva)، ومتوافقة مع قواعد دراسة الجدوى في المشروع (`templates/FEASIBILITY_TEMPLATE_SPEC.md` و `FEASIBILITY_QA_STANDARDS.md`).

---

## 0. الهدف من الخطة (Objectives)

هذه الخطة تستهدف تحسين التجربة دون المساس بجودة القرار المالي أو كسر معايير القالب.

### أ. أهداف قابلة للقياس

| الهدف | خط الأساس (Baseline) | الهدف (Target) | نافذة الزمن |
|------|------------------------|----------------|------------|
| **رفع معدل إكمال الوصول لصفحة النتائج** | يُقاس لاحقاً | +25% | 6–8 أسابيع بعد إطلاق المرحلة 1 |
| **تقليل وقت الوصول لأول نتيجة** | يُقاس لاحقاً | المسار السريع: < 7 دقائق | بعد إطلاق المرحلة 1 |
| **تقليل أخطاء QA المرتبطة بالمدخلات** | يُقاس لاحقاً | -40% | 8–12 أسبوع |
| **رفع الثقة** (تقليل التوقف عند الحقول الصعبة) | يُقاس لاحقاً | -30% في معدلات التوقف/الانسحاب عند الحقول الصعبة | 8–12 أسبوع |

### ب. مبادئ تصميم (Design Principles)

1. **القرار أولاً:** عرض النتيجة والتوصية مبكراً، ثم فتح التفاصيل للتعديل.
2. **الافتراضي ذكي، والخبير يتحكم:** إعدادات افتراضية موثوقة + مسار تفصيلي كامل.
3. **تعليم داخل السياق:** مساعدة قصيرة بجانب الحقل، مع مثال وحساب وحدة واضح.
4. **أخطاء أقل بالتوجيه:** Units، placeholders، حدود منطقية، والتحقق أثناء الإدخال.
5. **شفافية التقدير:** أي رقم مُقترح يجب أن يوضح مصدره ولماذا هو مناسب.

---

## 1. المشاكل الحالية (Pain Points)
*   **الإغراق المعرفي (Cognitive Overload):** وجود أكثر من 30 خطوة و 80 قسم فرعي يصيب المستخدم بالشلل (Analysis Paralysis).
*   **فقدان الثقة "متلازمة الصفحة البيضاء":** المستخدم يخشى إدخال أرقام مثل "سعر الصرف" أو "نسبة الهالك" خوفاً من إفساد الدراسة.
*   **تجربة خطية جامدة:** النظام يجبر الخبير والمبتدئ على نفس المسار الطويل.

---

## 2. الحلول المقترحة (The Solution)
رحلة المستخدم الجديدة (The New Journey)

### أ. المسار السريع: "ابدأ بـ 5 أرقام فقط" (Express Mode) 🚀
**الهدف:** منح المستخدم "مسودة أولية" متكاملة في أقل من 5 دقائق باستخدام ذكاء السوق.
*   **آلية العمل:**
    1.  يختار المستخدم: (القطاع، المدينة، المساحة، مستوى التجهيز).
    2.  يقوم **"محرك ذكاء السوق" (Market Intelligence Engine)** بسحب البيانات:
        *   متوسط الإيجارات في الحي المختار (من قاعدة البيانات).
        *   متوسط الرواتب (من بيانات وزارة الموارد البشرية المحفوظة).
        *   تكاليف التأسيس التقريبية (من دراسات سابقة مماثلة).
    3.  **النتيجة:** يحصل المستخدم على دراسة مبدئية جاهزة للتعديل بدلاً من البدء من الصفر.

### ب. المساعد الذكي "أكمل عني" (Magic Fill & Smart Benchmarks) ✨
**الهدف:** القضاء على حيرة "كم أضع هنا؟".
*   **الميزة:** زر "عصا سحرية" بجوار الحقول الصعبة (مثل: فواتير الكهرباء، تكلفة التسويق).
*   **التقنية:** يرسل النظام مواصفات المشروع إلى **"محرك الخبرة" (Experience Engine)** الذي يرد بـ: *"لمشروع مقهى 100م في الرياض، المشاريع المماثلة تدفع عادة بين 1500 - 2000 ريال"*.
*   **التفاعل:** ضغطة واحدة تملأ الحقل بالمتوسط، مع تمييز الرقم بلون مختلف ليعرف المستخدم أنه "تقديري".

### ج. واجهة "التركيز" (Focus Mode UI) 🔍
**الهدف:** تبسيط الشاشات المزدحمة.
*   **التبويب الذكي:** إخفاء التفاصيل المحاسبية المعقدة (الإهلاك، التدفقات المخصومة) في تبويب "تحليل متقدم"، وإبقاء الأرقام الحيوية (الربح الصافي، رأس المال المطلوب) في الواجهة الرئيسية.
*   **التدرج في المعلومات (Progressive Disclosure):** لا تظهر الأسئلة التفصيلية (مثل تفاصيل التأمينات الاجتماعية) إلا إذا اختار المستخدم خياراً يستدعيها.

### د. التلعيب والمكافأة (Gamification Implementation) 🏆
*   **مؤشر جودة الدراسة (Study Health Score):** عداد (0-100%) يرتفع كلما أدخل المستخدم بيانات دقيقة وليس تقديرية.
*   **نقاط الإنجاز:** رسائل تفاعلية عند إكمال الأقسام الحيوية ("أحسنت! أكملت هيكل التكاليف، أنت في منتصف الطريق").

---

## 3. الهيكل التقني للتنفيذ (Technical Implementation)

### المرحلة 1: تكامل الواجهة مع محركات الذكاء (Frontend-Backend Integration)
*   ربط واجهة المستخدم بـ `market_engine.py` لجلب البيانات الافتراضية عند بدء الدراسة.
*   ربط أزرار "Magic Fill" بـ `experience_engine.py` للحصول على التوصيات اللحظية.

### المرحلة 2: إعادة تصميم "الويزارد" (Wizard Refactoring)
*   تحويل النموذج الحالي الطويل إلى **SPA (Single Page Application)** سلس مع انتقالات ناعمة (Transitions).
*   استخدام `localStorage` للحفظ التلقائي اللحظي (Auto-save) لتجنب فقدان البيانات.

---

## 4. خارطة الطريق (Implementation Roadmap)

### الأسبوع 1: الأساس الذكي
- [ ] بناء API لجلب "القيم الافتراضية" (Defaults) من `market_engine`.
- [ ] تصميم واجهة "المسار السريع" (Express Mode Input Screen).

### الأسبوع 2: المساعد التفاعلي
- [ ] برمجة وظيفة "Magic Fill" لكل الحقول الرقمية.
- [ ] إضافة التلميحات الذكية (Tooltips) التي تشرح المصطلحات الصعبة.

### الأسبوع 3: تحسين المظهر
- [ ] تطبيق تصميم "Focus Mode" وإخفاء الأعمدة الثانوية.
- [ ] إضافة الرسوم البيانية التفاعلية (Live Charts) التي تتغير فوراً مع تغيير الأرقام.

## 5. مقاييس النجاح (KPIs)
| المقياس | خط الأساس (Baseline) | الهدف (Target) | كيفية القياس |
|---------|------------------------|----------------|--------------|
| **Time-to-First-Result** (الوقت حتى أول نتيجة) | يُقاس لاحقاً | المسار السريع: Median < 7 دقائق | زمن من بدء الجلسة إلى ظهور صفحة النتائج |
| **Completion Rate** (معدل الإكمال) | يُقاس لاحقاً | +25% خلال 6–8 أسابيع من إطلاق المرحلة 1 | عدد الجلسات التي تصل للنتيجة / عدد الجلسات |
| **Drop-off at Hard Fields** (الانسحاب عند الحقول الصعبة) | يُقاس لاحقاً | -30% | مقارنة نقاط الخروج قبل/بعد + عدد استخدام Magic Fill |
| **Input Validation Errors** (أخطاء التحقق) | يُقاس لاحقاً | -40% | عدد مرات منع الإدخال/تصحيح المدخلات |
| **Adoption of Express Mode** (تبني المسار السريع) | يُقاس لاحقاً | ≥ 40% عند عرضه للمستخدم | نسبة من اختار Express من شاشة البداية |

## 6. خطوات التنفيذ الفوري (Next Steps)

1. **تحديد نطاق الـ Express Mode بدقة:** ما هي 5–7 مدخلات الإلزامية التي تنتج دراسة قابلة للعرض دون كسر QA.
2. **توحيد مصدر القيم الافتراضية:** تعريف مصدر موحد لـ Benchmarks (حتى لو كان مؤقتاً JSON/جدول) قبل ربطه بالواجهة.
3. **إطلاق نسخة أولى من Magic Fill للحقول الأعلى تعثراً:** البدء بـ (الإيجار/الرواتب/الكهرباء/التسويق) ثم التوسع.
4. **تحسين التوجيه داخل السياق:** وحدة واضحة + مثال + نطاق منطقي لكل حقل حساس.
5. **إضافة قياس بسيط منذ اليوم الأول:** تسجيل (اختيار الوضع/إكمال Express/عرض النتائج/استخدام Magic Fill) لمقارنة قبل/بعد.

---

## 7. المخاطر والقيود (Risks & Constraints)

| الخطر | الاحتمال | التأثير | التخفيف |
|-------|----------|---------|----------|
| التقديرات الافتراضية لا تناسب مشاريع ذات خصوصية عالية | متوسط | قرارات غير دقيقة أو انخفاض ثقة | توضيح أنها "تقديرية" + إبراز إمكانية التعديل + إبراز السيناريوهات والحساسية |
| تضخم التعقيد البرمجي بسبب تعدد المحركات/الواجهات | متوسط | صيانة أصعب | فصل طبقة التقدير عن الواجهة + واجهات واضحة (API/Services) |
| قياس غير كافٍ للتحقق من نجاح التحسين | متوسط | صعوبة إثبات الأثر | الالتزام بأحداث قياس قليلة وواضحة + مراجعة أسبوعية |
| كسر جودة المخرجات أو QA بعد التعبئة التلقائية | متوسط | دراسة غير صالحة | تحقق إدخال صارم + اختبارات تلقائية على مخرجات القالب |
