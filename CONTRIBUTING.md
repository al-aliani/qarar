# المساهمة في محاكي الجدوى

شكراً لاهتمامك بالمساهمة. لإضافة تحسينات أو إصلاح أخطاء:

## البيئة

- Node.js 18+
- npm أو yarn

```bash
npm install
npm run dev
```

## الانضباط في دورة التطوير (CI/CD)

يخضع هذا المشروع لسياسة تطوير صارمة لمنع الانحدار (Regression) وحماية النزاهة المالية:
- **منع الدمج المباشر (Merge Freeze):** يُمنع تماماً الدفع المباشر (`Push`) إلى فرع `main`.
- **الفروع المستقلة:** أي ميزة أو إصلاح يجب أن يتم في فرع منفصل (`feature/` أو `fix/`).
- **بيئة المراجعة (Staging):** يجب أن يمر كل كود ببيئة Staging لمراجعته.
- **تخطي الاختبارات:** يُشترط اجتياز كافة اختبارات GitHub Actions (Linting + Vitest). سيُحجب خيار الـ Merge آلياً إن فشل أي اختبار رياضي مالي.

## معايير الكود

1. **التزام بالقواعد**  
   راجع `templates/FEASIBILITY_QA_STANDARDS.md` و `templates/FEASIBILITY_TEMPLATE_SPEC.md` للدراسات المالية.

2. **لا أرقام هاربة**  
   استخدم Named Ranges / مراجع محددة في الحسابات، وتجنّب `#REF!` و `OFFSET` و `INDIRECT` في النماذج المالية.

3. **اللغة**  
   الواجهة والمخرجات بالعربية. التعليقات والوثائق الداخلية يمكن أن تكون بالإنجليزية أو العربية.

4. **الاختبارات**  
   أضف اختبارات Vitest للمنطق المالي والتحقق من البيانات:
   ```bash
   npm test
   ```

## هيكل المشروع

- `web/js/core/` — المحرك والبيانات
- `web/js/ui/` — مكونات الواجهة
- `web/export/` — مولدات التقارير والتصدير
- `docs/` — الوثائق

راجع `docs/ARCHITECTURE.md` لفهم الهيكل العام.
