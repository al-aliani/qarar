# Template definitions (generated)

هذا المجلد يحتوي ملفات `definition.json` الناتجة من سكربت الاستخراج:

- `scripts/extract-excel-templates.ts`

المصدر:
- `/assets/templates/excel/`

المخرجات:
- `/assets/templates/definitions/*.definition.json`

