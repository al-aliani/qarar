/**
 * بصمة مدخلات محاكاة مونت كارلو (تدقيق 2026-09-16): تُحفظ مع كل تشغيل، وتُقارَن
 * وقت القراءة لتحديد ما إذا كانت النتيجة المحفوظة لا تزال مطابقة لمدخلات الدراسة
 * الحالية. قبل هذا كانت نتيجة قديمة (احتمالية نجاح محسوبة على مدخلات سابقة)
 * تدخل صامتة في الدرجة والتوصية بعد تغيير جوهري في المدخلات (مثل عدد العملاء)
 * حتى إعادة تشغيل يدوية.
 *
 * تُستثنى monteCarlo (دائرية ذاتية — البصمة نفسها تُخزَّن هناك) وresults (مخرَج
 * مشتق يُعاد كتابته في الحالة عند كل زيارة تقريباً؛ تضمينه يُنتج بصمة "قديمة"
 * زائفة عند كل عرض حتى بلا أي تغيير مدخلات فعلي من المستخدم).
 */
export function computeInputsFingerprint(state) {
    const { monteCarlo, results, ...rest } = state || {};
    return JSON.stringify(rest);
}
