/**
 * بانر تنبيه بصمة إصدار المحرك — منطق موحّد (single source) يُستخدم من كل الأسطح
 * التي تعرض ملخّص/قرار دراسة: ProjectOverviewView (الأصل، قرار لجنة 2026-08-27)،
 * وامتداداً لذلك ShareView وDecisionDashboard وExecutiveSummary (خطة إصلاح بانر
 * إصدار المحرك، بند 4، 2026-08-29). قبل هذا الملف كان الفحص مكتوباً مرة واحدة فقط
 * في ProjectOverviewView؛ توسيع التغطية لأكثر من سطح يعني تكراره — نفس منطق توحيد
 * escapeHtml في web/js/utils/escape.js بدل نسخة محلية بكل ملف.
 *
 * قرار لجنة 2026-08-27 الأصلي (انظر engine.js:ENGINE_VERSION): لا تنبيه بلا بصمة
 * محفوظة سابقاً في هذه الدراسة تحديداً (لا أساس مقارنة — دراسات قديمة قبل هذه
 * الميزة)، ولا حين تطابق البصمة المحفوظة الإصدار الحالي. تنبيه فقط حين نعرف فعلياً
 * أن المعادلات تغيّرت منذ آخر حفظ.
 */
import { ENGINE_VERSION } from '../core/engine.js';

/** هل بصمة إصدار المحرك المحفوظة في هذه الدراسة أقدم من الإصدار الحالي؟ */
export function isEngineVersionStale(data) {
    const savedVersion = data?._meta?.engineVersion;
    return !!savedVersion && savedVersion !== ENGINE_VERSION;
}

/**
 * تصحيح بند 5 (2026-08-29): النص السابق كان يدّعي أن إعادة الحفظ "تُثبّت" الأرقام
 * — ادّعاء غير صحيح. لا تجميد فعلي للنتائج في هذه المرحلة (تجميد النتائج الحقيقي
 * تصميم منفصل مؤجَّل عمداً — انظر engine.js:167-170). كل ما تفعله إعادة الحفظ
 * فعلياً هو تحديث بصمة الإصدار المحفوظة لتطابق الحالي، فيسكت هذا التنبيه حتى تغيّر
 * المحرك مجدداً — أما الأرقام نفسها فتُحسب حياً من جديد في كل عرض أو تصدير بإصدار
 * المحرك الحالي وقتها، سواء أُعيد الحفظ أم لا.
 */
export const ENGINE_VERSION_NOTICE_TEXT = 'تم تحديث معادلات المحرك المالي منذ آخر حفظ لهذه الدراسة — قد تختلف الأرقام المعروضة الآن عمّا رأيته أو صدّرته سابقاً. إعادة حفظ الدراسة تُحدِّث بصمة الإصدار المحفوظة فقط (فلن يتكرر هذا التنبيه حتى يتغيّر المحرك مجدداً) — ولا "تُثبّت" أي رقم: كل عرض أو تصدير يُعيد حساب الأرقام حياً بإصدار المحرك الحالي وقتها.';

/**
 * @param {object} data - بيانات الدراسة (تحوي _meta.engineVersion المحفوظة وقت آخر حفظ)
 * @returns {string} HTML جاهز للإدراج (صنف CSS مشترك `.engine-version-notice` في
 *   components.css)، أو '' إن لم يلزم تنبيه
 */
export function renderEngineVersionNotice(data) {
    if (!isEngineVersionStale(data)) return '';
    return `<div class="engine-version-notice" role="note">${ENGINE_VERSION_NOTICE_TEXT}</div>`;
}
