/**
 * إعداد الدفع بالتحويل البنكي — عدّل هذا الملف فقط. يُقرأ وقت التشغيل (public/ يُنسخ
 * كما هو، بلا إعادة بناء)، تماماً مثل whatsapp-config.js.
 *
 * ⚠️⚠️ قبل التفعيل (enabled: true) — تحقّق من الآيبان حرفاً-حرفاً من تطبيق «بنك البلاد» ⚠️⚠️
 *   رقم واحد خاطئ في الآيبان = تحويل أموال العميل إلى حساب آخر. القيمة أدناه قراءة
 *   مبدئية من شهادة الحساب وقد تحتوي خطأ — الصق الآيبان الرسمي وتأكّد منه ثم فعّل.
 *
 * التحويل البنكي قناة *يدوية*: يُنشئ طلباً بحالة «قيد الانتظار»، ثم تؤكّده أنت يدوياً
 * بعد وصول الحوالة (عبر لوحة الأدمن أو دالة admin_confirm_bank_transfer)، فيُفتح
 * التصدير للعميل تلقائياً.
 */
window.BANK_TRANSFER = {
    // اجعلها true فقط بعد التحقق من الآيبان أدناه.
    enabled: false,

    // اسم المستفيد كما هو مسجَّل على الحساب لدى البنك (يظهر للعميل لبناء الثقة).
    beneficiaryName: 'شركة شفق الأعمال التجارية',

    bankName: 'بنك البلاد',

    // ⚠️ تحقّق منه من تطبيق البنك قبل التفعيل — لا تعتمد على هذه القيمة كما هي.
    iban: 'SA0000000000000000000000',

    // اختياري (مرجعي فقط — العميل يحوّل بالآيبان):
    accountNumber: '',

    swift: 'ALBISARIXXX'
};
