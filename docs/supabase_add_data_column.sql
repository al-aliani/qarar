-- ============================================================
-- ⛔ ملف مُتقاعد (DEPRECATED) — لم يعد لازماً.
-- ============================================================
-- كان ترقيعاً يضيف عمود data لجدول studies. المخطط الكنسي الآن يُنشئ
-- studies بعمود data أصلاً، فلا حاجة لهذا الترقيع.
--
-- ✅ نفّذ فقط:  docs/supabase_setup.sql  (المخطط الكنسي الكامل).
-- ============================================================
