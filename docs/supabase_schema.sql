-- ============================================================
-- ⛔ ملف مُتقاعد (DEPRECATED) — لا تُنفّذه.
-- ============================================================
-- كان هذا الملف يعرّف جدول studies بعمود owner_id + جدول study_inputs،
-- وهو مخطط قديم يتعارض مع كود التطبيق (الذي يستخدم user_id + عمود data JSONB).
-- تشغيله يسبّب فشل الحفظ/التحميل السحابي.
--
-- ✅ المخطط الكنسي الوحيد الآن:  docs/supabase_setup.sql
--    نفّذه في: Supabase → SQL Editor → New query → الصق الكامل → Run.
--    يحوي: profiles + studies(user_id, data) + study_shares + study_versions
--    + المشغّلات، متسقاً تماماً مع web/js/services/PersistenceService.js.
-- ============================================================
