# إصلاح نقاط الضعف — 2026-01-27

تمت معالجة البنود المستخلصة من `استخراج_المشاكل_والأخطاء` و`مقارنة_مع_المنصات_العالمية`.

---

## ١. توحيد الحفظ ومصادِر التخزين

- **`web/js/core/store.js`**: توضيح استراتيجية التخزين في التعليقات:
  - `mac_blash_study_v2`: المسودة النشطة (قراءة/كتابة سريعة).
  - `PersistenceService` (`feas_project_<id>`): المشاريع المحفوظة + المزامنة السحابية مع Supabase عند تسجيل الدخول.
  - التوثيق يوضح أن الـ AutoSave يوجّه إلى ProjectManager → PersistenceService، وعدم استخدام `feas_autosave` القديم.

---

## ٢. الحفظ السحابي والمصادقة

- **`web/js/ui/AuthModalStub.js`**: استبدال الـ stub بواجهة تسجيل دخول/إنشاء حساب فعلية:
  - نموذج (بريد، كلمة مرور) مع أزرار "دخول" و"إنشاء حساب".
  - استدعاء `signIn` و`signUp` من `supabaseClient.js`.
  - إظهار رسالة عند عدم تهيئة Supabase (SUPABASE_URL، SUPABASE_ANON_KEY).
- **`web/index.html`**: إضافة `id="authModalContainer"` و`id="exportMenuOverlay"` لوضع النوافذ المنبثقة.
- **`web/js/ui/Sidebar.js`**: تبسيط `updateSaveStatus` وإزالة الاعتماد على `store.getAuthUser` غير الموجود.

---

## ٣. ربط أزرار التوليد بالذكاء الاصطناعي

- **`web/js/services/AIWriter.js`**: في حالة `swot`، تمرير `data` (معلومات المشروع) كمعامل ثالث إلى `connector.query(..., 'analysis', data)` حتى يصل `projectInfo` إلى الخادم.
- **`ai_server.py`**:
  - دعم `type` بقيمة `'analysis'` بالإضافة إلى `'swot'` في `generate_text` (لأن AIConnector/AIWriter يرسلان `type=analysis`).
  - إضافة معالجة لـ `prompt_type == 'summary'` لتحويلها إلى `executive_summary` حتى يعمل الملخص التنفيذي عبر الخادم المحلي.

الملخص التنفيذي وSWOT والتسويق/المنافسين/الشرائح معرّفة مسبقاً في المكوّنات وتستدعي AIConnector أو AIWriter؛ التعديلات تؤكد أن الطلبات تصل إلى الخادم المحلي بالمعاملات الصحيحة.

---

## ٤. توحيد التصدير

- **`web/index.html`**:
  - زر الموبايل: تغيير النص من "تصدير التقرير" إلى "📥 تصدير" ليتوافق مع الشريط الجانبي.
  - الزر يفتح نفس `ExportMenu` (PDF، Excel، تقرير بنكي، JSON) كما في `app.js`.
- قائمة التصدير الموحّدة (ExportMenu) كانت مُطبَّقة مسبقاً؛ زر الشريط وزر الموبايل يفتحانها.

---

## ٥. تصدير Excel بالقالب المعياري

- **`web/js/ui/ExportMenu.js`** (بدون تعديل): يحاول أولاً `exportExcel` مع `adaptRunFullModelForExcel`؛ عند الفشل يستخدم `excelExporter` (SheetJS). لا تغيير في المنطق.

---

## ٦. تكامل محاسبة (placeholder)

- **`web/index.html`**: إضافة سطر في الشريط الجانبي:  
  `"ربط محاسبة (QuickBooks، Xero): قريباً"`  
  كمكان احتياطي لواجهة التكامل لاحقاً.

---

## ٧. توحيد التسمية

- **`web/index.html`**: العنوان والعلامة كانا "محاكي الجدوى" مسبقاً.
- **`BETA_LAUNCH.md`**: استبدال "Feasibility Simulator" بـ "محاكي الجدوى" في العنوان.

---

## ٨. تلميحات TAM / SAM / SOM

- **`web/js/ui/MarketAnalysis.js`**:
  - استبدال العبارة تحت عنوان "حجم السوق (TAM-SAM-SOM)" بـ:  
    `"TAM = إجمالي حجم السوق | SAM = السوق المتاح لك | SOM = حصتك المستهدفة"`.
  - التعريفات التفصيلية (title و tooltip-icon) لـ TAM و SAM و SOM كانت موجودة؛ تم إضافة مرجع سريع في نفس البطاقة.

---

## ملخص الملفات المُعدَّلة

| الملف | التعديل |
|-------|---------|
| `web/js/core/store.js` | توضيح استراتيجية التخزين في التعليقات |
| `web/js/services/AIWriter.js` | تمرير `data` لـ `query(..., 'analysis', data)` في حالة SWOT |
| `ai_server.py` | دعم `analysis` و`summary` في `generate_text` |
| `web/js/ui/MarketAnalysis.js` | عبارة تذكيرية TAM/SAM/SOM |
| `web/js/ui/AuthModalStub.js` | تنفيذ واجهة تسجيل دخول/إنشاء حساب بالكامل |
| `web/js/ui/Sidebar.js` | تبسيط `updateSaveStatus` |
| `web/index.html` | زر تصدير موبايل، حاويات للنوافذ، سطر "ربط محاسبة"، إلخ |
| `BETA_LAUNCH.md` | توحيد التسمية إلى "محاكي الجدوى" |
| `docs/استخراج_المشاكل_والأخطاء.md` | قسم «تحديث 2026-01-27» يربط بالإصلاحات ويحدّث حالة البنود |

---

## ما يتبقى (خارج نطاق التعديلات الحالية)

- تشغيل خادم AI (`ai_server.py` على المنفذ 8080) وتجهيز Supabase (SUPABASE_URL، SUPABASE_ANON_KEY) لتفعيل الدخول والحفظ السحابي فعلياً.
- تنفيذ تكامل محاسبة حقيقي (QuickBooks، Xero) لاحقاً.
- إعادة بناء `web` (`npm run build`) للتحقق من عدم وجود أخطاء في الحزمة.
