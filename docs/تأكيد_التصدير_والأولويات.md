# تأكيد التصدير والأولويات (8.7 → 9.0)

## الأولوية 1 — مؤكّد

| البند | الحالة | الملاحظات |
|-------|--------|-----------|
| **BankReportGenerator.js** | ✅ موجود | `web/export/BankReportGenerator.js` — تقرير تمويل بنكي HTML. مُربوط من لوحة القرار (زر "📄 تصدير تقرير بنكي") وقائمة التصدير. |
| **PitchDeckExporter.js** | ✅ موجود | `web/export/PitchDeckExporter.js` — تصدير Pitch Deck كـ HTML. مُربوط من لوحة القرار (زر "📥 تصدير Pitch Deck") وقائمة التصدير. |
| **10 اختبارات E2E** | ✅ مضافة | `e2e/app.spec.js` (6) + `e2e/user-journey.spec.js` (4) = 10 اختبارات. |
| **تأكيد البريد** | ✅ مفعّل | رسالة "البريد غير مفعّل" + زر "إعادة إرسال رابط التأكيد" في AuthComponent و AuthModal عند خطأ Email not confirmed. |

## الأولوية 2 (أسبوعان) — 8.7 → 9.0

| البند | الحالة | الملاحظات |
|-------|--------|-----------|
| **OAuth بـ Google** | ✅ مفعّل | زر "تسجيل الدخول بـ Google" في AuthComponent و AuthModal؛ توثيق في `docs/إعداد_Supabase.md`. |
| **توثيق API (Swagger)** | ✅ أساس | `docs/openapi.yaml` — OpenAPI 3 وصف نقاط التكامل (Supabase Auth، Studies). يعرض في Swagger UI / Redoc. |
| **Push Notifications** | ✅ أساس | مستمع `push` و `notificationclick` في `web/public/sw.js`؛ توثيق في `docs/Push_Notifications.md`. |
| **Dark Mode كامل** | ✅ مفعّل | داكن / فاتح / تلقائي (يتبع النظام)؛ زر في الشريط الجانبي + `[data-theme]` في CSS. |
| **20 اختبار إضافي** | ✅ مضافة | `e2e/e2e-extended.spec.js` (20). إجمالي 30 اختبار E2E في `e2e/`. |

---

**آخر تحديث:** يناير 2026
