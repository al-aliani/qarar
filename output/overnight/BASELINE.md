# BASELINE — قبل عمل الليل
captured: 2026-08-25 23:48 local
HEAD: f78729e  (fix(engine): «امضِ» لمشروع خاسر — رصيد القرض يختفي حين تقصر مدته عن الأفق)

## حجم قاعدة الكود
js under web/js: 268 ملف، 65314 سطر
lib/: 13 ملف

## الاختبارات
ملفات اختبار: 3132
it()/test() blocks: 18651
e2e specs: 15

## dist (آخر بناء موجود)
assets: 188 ملف، إجمالي 8635061 بايت
  xlsxShim-Ci088bHX.js: 940484 B
  exceljs-prUwiUVQ.js: 940216 B
  exportWorker-V_pT0IEe.js: 863344 B
  main-CElXjN0G.js: 783477 B
  apexcharts-BSte8qSO.js: 622732 B

---

## جولة العميل — نتائج مؤكَّدة حياً (00:10)

### مؤكَّد: عيب فقدان بيانات صامت (الأخطر)
`web/js/utils/storageManager.js` — عند `QuotaExceededError` يكتب لـIndexedDB
ويُرجِع `success:true` بلا علامة `_ref` وبلا مسح النسخة القديمة من localStorage.
`getItem` بلا علامة يقرأ localStorage أولاً ⟶ يُعيد الأقدم. مسار حفظ الدراسة
الأساسي (`store.js:355`). صفر اختبارات. ⟶ وكيل يعمل عليه.

### مؤكَّد: أول انطباع خاطئ في قمع الشراء
`AuthModalStub.open()` يفتح دائماً على «دخول» + «أهلاً بعودتك»، رغم أن
`app.js:265-283` يخزّن `landing_cta`/`selected_package` أي يعرف أن الزائر جاء
ليبدأ. ⟶ وكيل يعمل عليه.

### مؤكَّد: فجوة وعد/تسليم
زر «شاهد نموذج تقرير أولاً» (ثاني أبرز زر في الصفحة) يقود إلى `#sample` =
بطاقة 6 أرقام موسومة «مثال توضيحي»، لا تقرير.

### مؤكَّد: نمو localStorage بلا سقف عملي
`qarar_version_history` 103KB + `feas_audit_log` 108KB = 211KB لمستخدم محلي
بمشروع واحد صغير. السقف 10 إصدارات/500 قيد — لكن حجم اللقطة يكبر مع الدراسة.

### إنذار كاذب من عندي — لا يُطارَد ثانية
قِستُ أولاً «30/77 حقلاً بلا اسم متاح، 15 زراً، 7 عناوين فارغة». **كله خاطئ.**
السبب: `innerText` يُرجع '' لعناصر لها صندوق تخطيط لكن `visibility:hidden`.
القياس الصحيح بـ`textContent` + `getComputedStyle`:
  77 حقلاً ⟶ 1 بلا اسم (`contractRiskScanFile`) + 1 placeholder فقط (`contractRiskScanText`)
  155 زراً ⟶ 0 بلا اسم
  35 عنواناً ⟶ 0 فارغ، تخطٍّ واحد 1→3 عند «أسئلة تأهيلية»
**قاعدة: أي فحص a11y في هذا المشروع يستخدم textContent + getComputedStyle، لا innerText.**

### قرار مالك متعمد — ليس خللاً
`app.js:2011-2017`: الدخول إلزامي للوحة، وإغلاق نافذة الدخول يُعيد لصفحة التسويق.

### بند صيانة
16 worktree عالقاً في `.claude/worktrees/` من وكلاء سابقين.

---

## سجل القياسات — قبل/بعد (يُحدَّث مع كل دورة)

| القياس | قبل الليلة | بعد |
|---|---|---|
| اختبارات خضراء | 2,267 | 2,297+ |
| فجوة `opex.fixedAnnual` مقابل قائمة الدخل (مقهى، افتراضي 5%) | 43,022 | 0 |
| فجوة منحدر DSO في NPV (5,000 عميل) | 131,553 | 55,552 |
| أهداف لمس تفشل WCAG 2.2 §2.5.8 على 375px (خطوة ٠) | 83 / 221 | **11 / 221** |
| خانات «نموذج العمل» بشرح خاطئ أو حشو | 8 / 9 | 0 / 9 |
| فقد بيانات صامت عند امتلاء التخزين | نعم، بلا اختبار | مُصلَح، 5 اختبارات |
| تباين فاشل في التطبيق (فاتح، معالج + نتائج) | — | **0 من 484 عنصراً** |

### قياسات تحقّقت أنها سليمة (لا تُطارَد)
- تباين الوضع الفاتح في التطبيق: صفر فشل عبر 484 عنصراً نصياً (المعالج 309، النتائج 175).
- الجداول على الجوال: 9 جداول أعرض من المنفذ، **كلها** داخل حاويات `overflow-x:auto` تنزلق فعلاً. صفر محتوى غير قابل للوصول.
- أسماء متاحة: 77 حقلاً ⟶ 1 ناقص؛ 155 زراً ⟶ 0.

### إنذارات كاذبة من أدواتي أنا — موثّقة حتى لا تُعاد
1. `innerText` يُرجع '' لعناصر `visibility:hidden` ⟶ «30 حقلاً و15 زراً و7 عناوين» كاذبة.
2. فاحص التباين تسلّق فوق عنصر خلفيته `linear-gradient` و`background-color` شفاف ⟶ «نص أبيض على أبيض» كاذب للزر الأساسي.
3. «226 عنصراً يتجاوز المنفذ» ⟶ كلها داخل حاويات تنزلق. لا عطل.
**القاعدة**: `textContent` + `getComputedStyle`، واستثناء أي عنصر عليه `background-image` من حساب التباين.

### مزلق منهجي التُقط الليلة
اختبار زر اللمس مرّ **أخضر** بينما المتصفح ما زال يعرض 22px (وحدة مخزَّنة).
الاختبار وحده لا يُثبت وصول الإصلاح للمستخدم — التحقق الحيّ بعد إعادة تحميل نظيفة ملزم.

---

## دورة ٣ — تحقق من المخرَج على دراسة سعودية واقعية كاملة

بنيتُ «مقهى رواق» بالرياض من `createEmptyStudy()` الحقيقي: معدات 190,000، أثاث 180,000،
تجهيز 120,000، أربعة موظفين، إيجار 22,000 شهرياً، 4,200 عميل بمتوسط 24 ريالاً،
قرض 300,000 بفائدة 8% لخمس سنوات + ملكية 552,248.

### النتيجة: المحرّك ومخرَجه متماسكان
- `financingCheck`: قرض 300,000 + ملكية 552,248 = الاستثمار 852,248، **فجوة تمويل صفر**.
- جدول القرض يُطفأ إلى صفر في السنة الخامسة، وفائدة السنة الأولى 23,532 **تظهر في قائمة الدخل بنفس القيمة**.
- `dscrAnalysis` مكتمل خمس سنوات: 2.37 / 2.00 / 2.49 / 3.00 / 3.54 — كلها «مريح للممول».
- الميزانيات الخمس `isBalanced: true`.
- الرافعة ترفع IRR من 12.8% (بلا قرض) إلى 15.5% — سلوك متسق لا مصادفة.
- القرار `REVISE` وسببه الوحيد المتبقي «الاسترداد ≤ 3.5 سنوات» — متسق مع اجتياز IRR للعتبة.

### جودة `decisionExplanation` عالية فعلاً
ستة أسباب، كل واحد بعنوان وشرح رقمي وعتبة وقيمة و**مسار الحقل** الذي يجب تعديله
وإجراء عملي. مثال: «هامش الأمان قبل التعادل 3.6% فقط (١٬١٦٦٬٢٢٩ مقابل إيراد
١٬٢٠٩٬٦٠٠)». وإفصاحات صريحة أن الزكاة تقديرية وتحتاج مختصاً. هذا مستوى استشاري
حقيقي لا حشو.

### أربعة إنذارات كاذبة إضافية — كلها أسماء حقول خاطئة عندي أنا
`indicators.decision` (الصحيح `r.decision`) · `netProfit` (الصحيح `netIncome`) ·
`breakEvenRevenue` (الصحيح `breakEvenPointValue`) · `minDSCR` (الصحيح `indicators.dscr`) ·
`annualSummary[].interest` (الصحيح `totalInterest`) · `dscrAnalysis.byYear` (هي **مصفوفة**) ·
و`sources.loanAmount` (الصحيح `sources.bankLoan.amount`).
**المجموع الليلة: سبعة إنذارات كاذبة، كلها من أدواتي وافتراضاتي لا من المنتج.**

---

## دورة ٤ — مكتبة «دراسات جدوى جاهزة»: أصل المحتوى (قرار مالك مطلوب)

المكتبة نُظّفت سابقاً من 456 إلى 301 لتكون «سعودية 100%». التنظيف صفّى **البيانات
الوصفية** (حقل `country`) لا **محتوى الملفات**. فحصتُ الـ301 ملف PDF بفكّ ضغط
تدفقاتها واستخراج نصّها.

### مؤكَّد بقراءة المحتوى (ثلاث عيّنات فُتحت وقُرئت)
1. `Filipino traditional delicacie.pdf` — وسم `en-PH` داخلي، ومحتواه مشروع فلبيني
   لإنتاج `polvoron` و`pastillas` يُباع في «الأسواق العامة».
2. `Production-jameed.pdf` — «Al Jamid and dairy products — **Al Karak Governorate**»،
   «KEY HIGHLIGHT OF **JORDAN**»، «**JORDAN INVESTMENT COMMISSION** AIMS TO». سنة 2017.
3. `Volcaninc-Tuff-.pdf` — «Quarry to Extract Volcanic Tuff Ores — **Tafilah**».

الكرك والطفيلة ومعان محافظات أردنية، والجميد منتج أردني بامتياز. هذه سلسلة
منشورات **هيئة الاستثمار الأردنية**، لا دراسات سعودية.

### الأدلة بمستويات ثقتها (لا أخلطها)
| المستوى | العدد | الدليل |
|---|---|---|
| **قاطع** — الأردن | 22 | الأرقام المالية نفسها مقوّمة بالدينار الأردني داخل الجداول («9390.50 **JOD** Million»)، مع JORDAN/Karak/Tafilah؛ اثنتان قُرئتا كاملتين |
| **متوسط** — الفلبين | ~23 | 1–2 إشارة نصية؛ واحدة تحققتُ منها قطعياً بالقراءة |
| **ضعيف** — مصر | 46 | وسم لغة `ar-EG` فقط داخل الملف، بلا دليل نصي. قد يكون أثر برنامج التحرير لا المحتوى |
| — | 1 | الهند |

رمز `ar-SA` يظهر في **ملفَّين فقط** من 301 — وكلاهما داخل وثيقتين أردنيتين، أي أن
الوسوم نفسها غير موثوقة والمحتوى وحده حاسم.

البيان الكامل بالمقتطفات: `output/overnight/READY_STUDIES_ORIGIN.md`

**لا أحذف شيئاً.** إزالة ثلث مكتبة معروضة للعملاء قرار منتج يخصّك — أقدّم الدليل فقط.
الأولوية في رأيي: الاثنان والعشرون الأردنية (دليل قاطع ومنشور حكومي أجنبي باسمه).

### قياس جانبي: حزمة النشر
`web/dist` = **302 ميغابايت**، منها 259.6 للدراسات (301 PDF) و19.1 لقواعد البيانات
(ملف xlsx واحد 13.8 ميغابايت). كل نشر يرفع هذا كاملاً.
الحمولة الأولية للصفحة: 1,400,352 خام / **385,367 مضغوطة gzip** عبر 9 ملفات.
